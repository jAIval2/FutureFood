
  # Food Ordering Platform UI

  This is a code bundle for Food Ordering Platform UI. The original project is available at https://www.figma.com/design/r8h9T2ucsw4DAF63nzVjRy/Food-Ordering-Platform-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  